2.1.0 Patch Notes:

1) OnNavigationCompleted will now return the string of the url back as the EventArg parameter
2) Android now supports URI schemes with mailto:
3) iOS now supports URI schemes with mailto:

